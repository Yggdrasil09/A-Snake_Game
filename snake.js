function setup()
{
    var canvas1=createCanvas(600, 600);
    background(0, 0, 0);
    canvas1.position((windowWidth - width) / 2,(windowHeight - height) / 2);
}

function draw()
{
    rect(100,200,60,70);
}